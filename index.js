'use strict';

module.exports = Object.assign({}, {
    createIterator: require('./src/transform-consume'),   //eslint-disable-line
    startPermutation: require('./src/permutation')        //eslint-disable-line
});
